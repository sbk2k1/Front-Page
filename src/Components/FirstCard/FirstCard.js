import React from 'react'
import styles from './FirstCard.module.css'

const FirstCard = () => {
    return (
        <div className={styles.parent}>
            <h1>Arcade</h1>
            <p>All platforms like iOS, Android and also on browsers like chrome, Safari and so on.all platforms like iOS, Android and also on browsers like chrome, Safari and so on.all platforms like iOS, Android and also on browsers like chrome, Safari and so on.all platforms like iOS, Android and also on browsers like chrome, Safari and so on.</p>
        </div>
    )
}

export default FirstCard
